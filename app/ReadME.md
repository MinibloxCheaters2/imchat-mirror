# IMChat App (the Discord bot part of IMChat)

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This part is also written in Bun for the sake of simplicity and not needing to install the 500th JavaScript runtime.
